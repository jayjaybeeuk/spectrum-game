# Copy to .env (git-ignored) and fill in to enable Microsoft Clarity analytics.
# Clarity only loads in production builds (yarn build) and only after the
# visitor accepts the cookie consent banner. Leave blank to disable entirely.
VITE_CLARITY_ID=
